{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && pip install -r requirements.txt && python manage.py collectstatic --noinput && cd ../frontend && npm ci --legacy-peer-deps && npm run build"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn companion_api.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 60",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
